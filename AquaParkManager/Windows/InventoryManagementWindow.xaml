<Window x:Class="AquaParkManager.Windows.InventoryManagementWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
    Title="Inventory Management" Height="700" Width="1100" WindowStartupLocation="CenterScreen" Background="White">
    <Grid Margin="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{StaticResource HeaderBrush}" Padding="15">
            <StackPanel>
                <TextBlock Text="📦 Inventory Management" Style="{StaticResource HeaderTitleStyle}"/>
                <TextBlock Text="Monitor inventory levels, suppliers and low stock alerts" Style="{StaticResource HeaderSubTitleStyle}" Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <Grid Grid.Row="1" Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Search and Filters -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                <TextBlock Text="Search:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBox x:Name="txtSearch" Width="300" TextChanged="TxtSearch_TextChanged"/>
                <CheckBox x:Name="chkLowStock" Content="Show Low Stock Only" Margin="20,0,0,0" Checked="ChkLowStock_Checked" Unchecked="ChkLowStock_Unchecked"/>
            </StackPanel>

            <!-- DataGrid -->
            <DataGrid Grid.Row="1" x:Name="dgInventory" AutoGenerateColumns="False" 
                  IsReadOnly="True" SelectionMode="Single" SelectionChanged="DgInventory_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding ItemId}" Width="50"/>
                <DataGridTextColumn Header="Item Name" Binding="{Binding Name}" Width="200"/>
                <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="120"/>
                <DataGridTextColumn Header="Quantity" Binding="{Binding Quantity}" Width="80"/>
                <DataGridTextColumn Header="Min Stock" Binding="{Binding MinStock}" Width="80"/>
                <DataGridTextColumn Header="Unit Price" Binding="{Binding UnitPrice, StringFormat='{}{0:C}'}" Width="100"/>
                <DataGridTextColumn Header="Supplier" Binding="{Binding Supplier.Name}" Width="150"/>
                <DataGridTemplateColumn Header="Status" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding StockStatus}" Foreground="{Binding StatusColor}" FontWeight="Bold"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

            <!-- Form -->
            <Border Grid.Row="2" BorderBrush="Gray" BorderThickness="1" Padding="15" Margin="0,20,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Column="0" Grid.Row="0" Margin="0,0,10,10">
                    <TextBlock Text="Item Name:*"/>
                    <TextBox x:Name="txtItemName"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Grid.Row="0" Margin="0,0,10,10">
                    <TextBlock Text="Category:"/>
                    <TextBox x:Name="txtCategory"/>
                </StackPanel>

                <StackPanel Grid.Column="2" Grid.Row="0" Margin="0,0,0,10">
                    <TextBlock Text="Supplier:"/>
                    <ComboBox x:Name="cmbSupplier" DisplayMemberPath="Name" SelectedValuePath="SupplierId"/>
                </StackPanel>

                <StackPanel Grid.Column="0" Grid.Row="1" Margin="0,0,10,10">
                    <TextBlock Text="Quantity:*"/>
                    <TextBox x:Name="txtQuantity"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Grid.Row="1" Margin="0,0,10,10">
                    <TextBlock Text="Minimum Stock:*"/>
                    <TextBox x:Name="txtMinStock"/>
                </StackPanel>

                <StackPanel Grid.Column="2" Grid.Row="1" Margin="0,0,0,10">
                    <TextBlock Text="Unit Price:"/>
                    <TextBox x:Name="txtUnitPrice"/>
                </StackPanel>

                <StackPanel Grid.Column="0" Grid.ColumnSpan="3" Grid.Row="2" Margin="0,0,0,0">
                    <TextBlock Text="Notes:"/>
                    <TextBox x:Name="txtNotes" Height="60" TextWrapping="Wrap" AcceptsReturn="True"/>
                </StackPanel>
            </Grid>
        </Border>

            <!-- Buttons -->
            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,20,0">
                <Button x:Name="btnAdd" Content="➕ Add New" Width="110" Height="30" Margin="0,0,10,0" Click="BtnAdd_Click" Style="{StaticResource AddButtonStyle}"/>
                <Button x:Name="btnUpdate" Content="💾 Update" Width="100" Height="30" Margin="0,0,10,0" Click="BtnUpdate_Click" Style="{StaticResource PrimaryActionButtonStyle}"/>
                <Button x:Name="btnDelete" Content="🗑️ Delete" Width="100" Height="30" Margin="0,0,10,0" Click="BtnDelete_Click" Style="{StaticResource DangerButtonStyle}"/>
                <Button x:Name="btnClear" Content="🔄 Clear" Width="100" Height="30" Click="BtnClear_Click" Style="{StaticResource NeutralButtonStyle}"/>
        </StackPanel>

        </Grid>

        <StatusBar Grid.Row="2" Background="{StaticResource StatusBarBrush}">
            <StatusBarItem>
                <TextBlock x:Name="lblStatus" Text="Ready"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
